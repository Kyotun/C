
#include <stdio.h>

#define leange 10
int main() {
    int a=-10,b=-5,c=-1,min;


    if (a<b){
    	if (a<c){
    		min = a;
    	} else{
    		min = c;
    	}
    } else if(b < c){
    	min = b;
    } else{
    	min = c;
    }
    printf("Minimum:%d\n",min);

    return 0;
}
