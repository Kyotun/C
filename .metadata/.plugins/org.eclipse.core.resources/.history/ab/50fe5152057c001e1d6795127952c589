#include <stdio.h>
#define N 2 /* fuer eine NxN-Matrix */

int main() {
	int a = 0;
	int b = 0;

	b = 1 + ++a;
	printf("b: %d", b);
	printf("\na: %d", a);

	b = a = 0;
	b = 1 + a++;
	printf("\n\nb: %d", b);
	printf("\na: %d", a);

  return 0;
}
