#include <stdio.h>
#define N 2 /* fuer eine NxN-Matrix */

int main() {
	int *p;
	int **q;
	int variable = 100;

	p = &variable;

	printf("*p mit prozent p: %p\n", *p);
	printf("*p mit prozent d: %d\n", *p);
	return 0;
}
